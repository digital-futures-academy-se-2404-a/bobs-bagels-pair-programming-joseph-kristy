export const assertEqual = (expected, actual) => actual === expected;
export const assertTrue = (actual) => assertEqual(true, actual);
export const assertFalse = (actual) => assertEqual(false, actual);
